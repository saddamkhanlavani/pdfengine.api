global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'pdfengine'
    static_configs:
      # The container, and the host process a developer runs, so one config covers both.
      - targets: ['api:8080', 'host.docker.internal:5276']
